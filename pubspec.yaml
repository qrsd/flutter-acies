name: lp_calc
description: lp calc
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  equatable: ^1.0.0
  flutter_bloc: ^2.0.1
  dartz: ^0.8.8
  shared_preferences: ^0.5.4+5
  flushbar: ^1.9.1
  preload_page_view: ^0.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/dice/
    - assets/coins/
    - assets/scroll/

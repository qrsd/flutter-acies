name: lp_calc
description: lp calc
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  equatable: ^0.6.1
  flutter_bloc: ^2.0.0
  dartz: ^0.8.8
  shared_preferences: ^0.5.4+5
  flushbar: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/
